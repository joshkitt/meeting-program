# By default, env variables are only available to the express server

# To make values available universally (browser React app & server React app),
# update the `share` function in src/app/pages/_document

# the project is configured to include this .env file in git so it can be used to create
# defaults

# for secure env variables (sensitive usernames, passwords, etc),
# create a .env-secure file and it will be .gitignored
APP_NAME = My app
PORT = 3000
VARIABLE1 = My variable

# This is needed if you make universal requests to
# to same host. Change when deploy to CF
HOST = http://localhost:3000
